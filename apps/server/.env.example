# 复制本文件为 .env 并按需修改
# PostgreSQL 连接串（本地用 docker-compose 时即下方默认值）
DATABASE_URL="postgresql://cloth:cloth@localhost:5432/cloth_scan?schema=public"

# 服务端口
PORT=3000

# JWT 密钥（生产请改为随机长字符串）
JWT_SECRET="change-me-in-production"
